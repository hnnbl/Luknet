---
phase: 02-hero-typography
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/theme.css
  - src/app/components/ServicesSection.tsx
  - src/app/components/DifferentialsSection.tsx
autonomous: true
requirements:
  - COPY-01

must_haves:
  truths:
    - "Section headlines (h2) are visually bold and heavy — font-extrabold or font-black, not medium weight"
    - "Theme base heading weight is updated so headings default to bold, not medium"
    - "ServicesSection and DifferentialsSection h2 text is visually consistent in weight with HeroSection h1"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Updated base heading font-weight from medium to bold"
      contains: "font-weight-bold"
    - path: "src/app/components/ServicesSection.tsx"
      provides: "Section h2 with font-extrabold and section subheadline"
      contains: "font-extrabold"
    - path: "src/app/components/DifferentialsSection.tsx"
      provides: "Section h2 with font-extrabold and section subheadline"
      contains: "font-extrabold"
  key_links:
    - from: "src/styles/theme.css"
      to: "h1, h2, h3 base styles"
      via: "--font-weight-bold variable applied to heading selectors"
      pattern: "font-weight.*bold"
    - from: "ServicesSection.tsx"
      to: "theme.css h2 base"
      via: "font-extrabold Tailwind class overrides base weight"
      pattern: "font-extrabold"
---

<objective>
Establish bold typography consistency across section headlines by updating the base heading weight in theme.css and upgrading ServicesSection and DifferentialsSection h2 elements to `font-extrabold`.

Purpose: COPY-01 requires "bold typography and impactful headlines across the entire page." Currently the theme.css base sets h1/h2/h3 to `font-weight-medium` (500), and section headlines use `font-bold` (700). This plan upgrades to `font-extrabold` (800) across sections and aligns the base theme for consistency.

Output: Updated theme.css base heading weights + upgraded section h2 classes in two section components.
</objective>

<execution_context>
@C:/Users/jorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/styles/theme.css
@src/app/components/ServicesSection.tsx
@src/app/components/DifferentialsSection.tsx
</context>

<interfaces>
<!-- Current state to modify. Extracted from codebase. -->

From src/styles/theme.css (lines 25-26, 111-127):
```css
:root {
  --font-weight-medium: 500;
  --font-weight-normal: 400;
  /* NOTE: --font-weight-bold is NOT currently defined */
}

@layer base {
  h1 { font-weight: var(--font-weight-medium); }  /* currently 500 */
  h2 { font-weight: var(--font-weight-medium); }  /* currently 500 */
  h3 { font-weight: var(--font-weight-medium); }  /* currently 500 */
}
```

From ServicesSection.tsx (line 54):
```tsx
<h2 className="text-4xl md:text-5xl font-bold mb-4">
  <span className="bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
    Serviços
  </span>
</h2>
```
Note: gradient uses `from-blue-400` — should align to violet palette.

From DifferentialsSection.tsx (line 44):
```tsx
<h2 className="text-4xl md:text-5xl font-bold mb-4">
  <span className="text-white">Por que escolher a </span>
  <span className="bg-gradient-to-r from-orange-400 to-pink-500 bg-clip-text text-transparent">
    Luknet
  </span>
</h2>
```
Note: orange/pink gradient is intentionally different for this section — keep it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update theme.css base heading weights to bold</name>
  <files>src/styles/theme.css</files>
  <action>
    Two targeted changes to src/styles/theme.css:

    **Change 1 — Add `--font-weight-bold` token to :root:**
    In the `:root { }` block, after the `--font-weight-normal: 400;` line, add:
    ```css
    --font-weight-bold: 700;
    ```

    **Change 2 — Update h1, h2, h3 base weights:**
    In the `@layer base { }` block, change the font-weight for h1, h2, h3 from:
    ```css
    font-weight: var(--font-weight-medium);
    ```
    to:
    ```css
    font-weight: var(--font-weight-bold);
    ```
    Apply this change to h1, h2, and h3 selectors only. Leave h4, label, button, input unchanged.

    h4 remains at `var(--font-weight-medium)` — subheadings and labels should not be heavy.

    Do NOT touch any other part of theme.css. The keyframe animations, color tokens, neon tokens, and Tailwind @theme block are untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/jorda/Downloads/luknet/Luknet-main/Luknet-main" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Build passes with zero errors
    - `--font-weight-bold: 700` exists in :root
    - h1, h2, h3 selectors in @layer base use `var(--font-weight-bold)` not `var(--font-weight-medium)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade section h2 typography in ServicesSection and DifferentialsSection</name>
  <files>src/app/components/ServicesSection.tsx, src/app/components/DifferentialsSection.tsx</files>
  <action>
    **ServicesSection.tsx — two changes:**

    1. Upgrade the h2 from `font-bold` to `font-extrabold` and fix gradient to violet palette:
       Replace:
       ```tsx
       <h2 className="text-4xl md:text-5xl font-bold mb-4">
         <span className="bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
           Serviços
         </span>
       </h2>
       ```
       With:
       ```tsx
       <h2 className="text-4xl md:text-5xl font-extrabold mb-4 tracking-tight">
         <span className="bg-gradient-to-r from-violet-400 to-purple-500 bg-clip-text text-transparent">
           Serviços
         </span>{' '}
         <span className="text-white">Premium</span>
       </h2>
       ```

    2. Add a subheadline `<p>` after the h2 and before the divider line:
       ```tsx
       <p className="text-gray-400 text-lg max-w-2xl mx-auto mb-6">
         Soluções construídas para durar — não para entregar e esquecer.
       </p>
       ```

    **DifferentialsSection.tsx — one change:**

    Upgrade the h2 from `font-bold` to `font-extrabold`:
    Replace:
    ```tsx
    <h2 className="text-4xl md:text-5xl font-bold mb-4">
    ```
    With:
    ```tsx
    <h2 className="text-4xl md:text-5xl font-extrabold mb-4 tracking-tight">
    ```

    Keep the existing "Por que escolher a Luknet" text and orange/pink gradient exactly as-is. Only the font-weight class changes.

    Add a subheadline `<p>` after the h2 and before the divider line:
    ```tsx
    <p className="text-gray-400 text-lg max-w-2xl mx-auto mb-6">
      Não entregamos projetos. Entregamos resultados que ficam.
    </p>
    ```

    Do NOT change any other part of these files — only the h2 class attribute, the span content/gradient in ServicesSection, and the new subheadlines.
  </action>
  <verify>
    <automated>cd "C:/Users/jorda/Downloads/luknet/Luknet-main/Luknet-main" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Build passes with zero TypeScript errors
    - ServicesSection h2 uses `font-extrabold` and `from-violet-400` gradient
    - DifferentialsSection h2 uses `font-extrabold`
    - Both sections have a subheadline paragraph with premium copy
    - No `from-blue-400` remains in ServicesSection h2 gradient
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `grep "font-weight-bold" src/styles/theme.css` returns the token definition and h1/h2/h3 usages
3. `grep "font-extrabold" src/app/components/ServicesSection.tsx` returns a match on the h2
4. `grep "font-extrabold" src/app/components/DifferentialsSection.tsx` returns a match on the h2
5. `grep "from-blue-400" src/app/components/ServicesSection.tsx` returns no matches in the h2 area
</verification>

<success_criteria>
- theme.css defines `--font-weight-bold: 700` and h1/h2/h3 use it as the base weight
- ServicesSection and DifferentialsSection h2 elements use `font-extrabold` Tailwind class
- Both section h2 headlines have `tracking-tight` for premium typographic feel
- Both sections have a subheadline `<p>` with copy that positions Luknet's quality, not just describes services
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-typography/02-02-SUMMARY.md` following the summary template at @C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</output>
