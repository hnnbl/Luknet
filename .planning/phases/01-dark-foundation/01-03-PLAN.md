---
phase: 01-dark-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/components/Header.tsx
autonomous: true
requirements:
  - HEAD-01
  - HEAD-02

must_haves:
  truths:
    - "Header logo uses purple/violet gradient consistent with the new palette"
    - "Header nav links have purple neon hover state (not generic white)"
    - "Scrolled header has a dark glassmorphism background with purple-tinted border"
    - "Header CTA button uses the primary purple accent with neon glow effect"
    - "Mobile hamburger menu opens a dark overlay panel with the same purple-accented nav links"
    - "Header is fully functional and responsive at all viewport widths"
  artifacts:
    - path: "src/app/components/Header.tsx"
      provides: "Redesigned dark header with purple/violet neon aesthetic"
      exports: ["Header"]
  key_links:
    - from: "src/app/components/Header.tsx"
      to: "src/styles/theme.css"
      via: "uses --neon-purple and --primary CSS variables via Tailwind color tokens"
      pattern: "text-violet|bg-violet|border-violet"
---

<objective>
Redesign Header.tsx to match the dark futuristic aesthetic — purple/violet logo gradient, neon-accented nav links, glassmorphism scroll behavior, a "Fale Conosco" CTA button, and a polished mobile menu panel.

Purpose: The header is the first element users see and sets the tone for the entire page. It must communicate premium dark tech immediately.
Output: Updated Header.tsx — same scroll/navigation logic, fully redesigned visual treatment.
</objective>

<execution_context>
@C:/Users/jorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-dark-foundation/01-01-SUMMARY.md
@src/app/components/Header.tsx

<interfaces>
<!-- Color tokens available from plan 01-01 (theme.css) -->
<!-- Tailwind arbitrary values available: violet-400 (#a78bfa), violet-500 (#8b5cf6), violet-600 (#7c3aed) -->
<!-- motion library: import { motion, AnimatePresence } from 'motion/react' -->

Current Header.tsx exports and logic to PRESERVE:
- scrollToSection(id: string) — DOM smooth scroll function, keep exactly
- isScrolled state — scroll listener, keep exactly
- isMobileMenuOpen state — toggle logic, keep exactly
- Nav section IDs: 'home', 'servicos', 'noticias', 'contato' — keep exactly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Header with dark glassmorphism, purple neon accents, and polished mobile menu</name>
  <files>src/app/components/Header.tsx</files>
  <action>
Rewrite the visual layer of `src/app/components/Header.tsx`. All logic (scroll listener, scrollToSection, state) is preserved exactly. Only the JSX and styles change.

**Imports to add:**
```tsx
import { useState, useEffect } from 'react';
import { Menu, X } from 'lucide-react';
import { motion, AnimatePresence } from 'motion/react';
```

**Root header element:**
Replace the current className logic with:
```tsx
<header
  className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${
    isScrolled
      ? 'bg-black/80 backdrop-blur-xl border-b border-violet-500/20 shadow-[0_4px_30px_rgba(124,58,237,0.15)]'
      : 'bg-transparent'
  }`}
>
```

The scrolled state creates a glassmorphism effect: dark semi-transparent background, blur, a subtle violet-tinted bottom border, and a purple box-shadow glow below.

**Logo section:**
```tsx
{/* Logo */}
<div className="flex items-center gap-3 cursor-pointer" onClick={() => scrollToSection('home')}>
  <div className="w-10 h-10 rounded-xl bg-gradient-to-br from-violet-500 to-purple-700 flex items-center justify-center shadow-[0_0_20px_rgba(124,58,237,0.5)]">
    <span className="text-white font-bold text-xl tracking-tight">L</span>
  </div>
  <span className="text-xl font-bold bg-gradient-to-r from-violet-400 to-purple-300 bg-clip-text text-transparent">
    Luknet
  </span>
</div>
```

Changes from original: violet-500/purple-700 gradient (instead of blue/purple), added neon box-shadow on logo mark, font-bold (was semibold), violet-400/purple-300 gradient text (crisper contrast), cursor-pointer on the whole logo div for better UX.

**Desktop navigation:**
```tsx
<nav className="hidden md:flex items-center gap-8">
  {[
    { label: 'Home', id: 'home' },
    { label: 'Serviços', id: 'servicos' },
    { label: 'Notícias', id: 'noticias' },
    { label: 'Contato', id: 'contato' },
  ].map((item) => (
    <button
      key={item.id}
      onClick={() => scrollToSection(item.id)}
      className="relative text-gray-400 hover:text-violet-300 transition-colors duration-300 text-sm font-medium group"
    >
      {item.label}
      <span className="absolute -bottom-1 left-0 w-0 h-px bg-gradient-to-r from-violet-500 to-purple-400 transition-all duration-300 group-hover:w-full" />
    </button>
  ))}
</nav>
```

This creates nav links with a sliding underline reveal on hover — a premium interaction pattern. The underline is a purple gradient that expands from left to right on hover.

**CTA button (new — add after nav):**
```tsx
<button
  onClick={() => scrollToSection('contato')}
  className="hidden md:flex items-center gap-2 px-5 py-2.5 rounded-xl bg-gradient-to-r from-violet-600 to-purple-600 hover:from-violet-500 hover:to-purple-500 text-white text-sm font-semibold transition-all duration-300 shadow-[0_0_20px_rgba(124,58,237,0.4)] hover:shadow-[0_0_30px_rgba(124,58,237,0.7)] active:scale-95"
>
  Fale Conosco
</button>
```

The CTA has a purple gradient fill, glowing box-shadow, and on hover the shadow intensifies for a neon pulse effect. active:scale-95 gives tactile press feedback.

**Mobile menu button (hamburger):**
```tsx
<button
  onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
  className="md:hidden relative p-2 rounded-lg text-gray-400 hover:text-violet-300 hover:bg-violet-500/10 transition-all duration-200"
  aria-label="Toggle mobile menu"
>
  <AnimatePresence mode="wait" initial={false}>
    <motion.div
      key={isMobileMenuOpen ? 'close' : 'menu'}
      initial={{ rotate: -90, opacity: 0 }}
      animate={{ rotate: 0, opacity: 1 }}
      exit={{ rotate: 90, opacity: 0 }}
      transition={{ duration: 0.15 }}
    >
      {isMobileMenuOpen ? <X size={22} /> : <Menu size={22} />}
    </motion.div>
  </AnimatePresence>
</button>
```

The hamburger-to-X icon now transitions with a small rotation animation using AnimatePresence.

**Mobile navigation panel:**
Replace the current conditional render with an AnimatePresence-wrapped motion.nav:
```tsx
<AnimatePresence>
  {isMobileMenuOpen && (
    <motion.nav
      initial={{ opacity: 0, height: 0 }}
      animate={{ opacity: 1, height: 'auto' }}
      exit={{ opacity: 0, height: 0 }}
      transition={{ duration: 0.25, ease: 'easeInOut' }}
      className="md:hidden overflow-hidden"
    >
      <div className="py-4 px-2 flex flex-col gap-1 border-t border-violet-500/20 mt-2">
        {[
          { label: 'Home', id: 'home' },
          { label: 'Serviços', id: 'servicos' },
          { label: 'Notícias', id: 'noticias' },
          { label: 'Contato', id: 'contato' },
        ].map((item) => (
          <button
            key={item.id}
            onClick={() => scrollToSection(item.id)}
            className="text-gray-300 hover:text-violet-300 hover:bg-violet-500/10 transition-all duration-200 text-left px-4 py-3 rounded-lg text-sm font-medium"
          >
            {item.label}
          </button>
        ))}
        <div className="pt-2 border-t border-violet-500/10">
          <button
            onClick={() => scrollToSection('contato')}
            className="w-full px-4 py-3 rounded-xl bg-gradient-to-r from-violet-600 to-purple-600 text-white text-sm font-semibold shadow-[0_0_20px_rgba(124,58,237,0.4)]"
          >
            Fale Conosco
          </button>
        </div>
      </div>
    </motion.nav>
  )}
</AnimatePresence>
```

Mobile menu animates open/close with height and opacity. Each nav item has a hover background highlight. The CTA appears at the bottom of the mobile menu too, making it accessible on mobile.

**Overall component structure stays identical — only visual/JSX changes. Logic is preserved verbatim.**

Preserve: useState, useEffect, handleScroll, scrollToSection, window.addEventListener('scroll', handleScroll), return () => removeEventListener cleanup.
  </action>
  <verify>
    <automated>cd "C:/Users/jorda/Downloads/luknet/Luknet-main/Luknet-main" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build passes. Header.tsx imports motion and AnimatePresence. Logo has violet gradient. Nav links have sliding underline hover effect. CTA "Fale Conosco" button is visible on desktop. Mobile menu animates with AnimatePresence. Scroll behavior still triggers glassmorphism class.</done>
</task>

</tasks>

<verification>
Run `npm run build` — zero errors. Check Header.tsx: imports from 'motion/react', has 'backdrop-blur-xl' class on scrolled state, has AnimatePresence wrapping mobile nav, has 'Fale Conosco' button text. Visually at 375px viewport width: mobile hamburger visible, menu opens/closes with animation. At 768px+ desktop: horizontal nav visible with CTA button.
</verification>

<success_criteria>
- Header logo: violet-500/purple-700 gradient square mark + gradient text "Luknet"
- Scrolled state: bg-black/80 backdrop-blur-xl border-b border-violet-500/20 with purple shadow
- Desktop nav: 4 links with sliding purple underline on hover, "Fale Conosco" CTA button
- Mobile: hamburger/X icon with rotation animation, slide-open panel with same links + CTA
- All section IDs preserved: 'home', 'servicos', 'noticias', 'contato'
- npm run build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-dark-foundation/01-03-SUMMARY.md` with what was implemented, files changed, and key decisions made.
</output>
