---
phase: 04-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/NewsSection.tsx
autonomous: true
requirements:
  - SECT-01
must_haves:
  truths:
    - "NewsSection has zero blue/cyan color tokens — all accent colors are violet/purple/fuchsia"
    - "NewsSection cards enter the viewport with a visible stagger animation when the user scrolls to that section"
    - "NewsSection section header animates in (y slide-down) before the card grid stagger begins"
    - "NewsSection decorative lines use violet rather than blue/cyan"
    - "NewsSection 'Leia mais' button uses violet text, not blue"
  artifacts:
    - path: "src/app/components/NewsSection.tsx"
      provides: "Fully redesigned dark violet news section with scroll animations"
      contains: "motion/react"
  key_links:
    - from: "src/app/components/NewsSection.tsx"
      to: "motion/react"
      via: "import { motion } from 'motion/react'"
      pattern: "whileInView"
---

<objective>
Redesign NewsSection from blue/cyan to violet/purple brand palette and add whileInView scroll entrance animations using motion/react.

Purpose: The News section is currently visually disconnected from the violet identity established in Phases 1-3. Visitors scrolling past it would see an abrupt color break.
Output: src/app/components/NewsSection.tsx fully rewritten with dark violet aesthetic and staggered card entrance animations.
</objective>

<execution_context>
@C:/Users/jorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Key patterns from DifferentialsSection (Phase 3 reference implementation): -->
<interfaces>
<!-- Animation pattern used across the project (from DifferentialsSection.tsx): -->
```typescript
import { motion } from 'motion/react';

const sectionVariants = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.15 } },
};

const itemVariants = {
  hidden: { opacity: 0, x: -30 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.6, ease: 'easeOut' } },
};

const headerVariants = {
  hidden: { opacity: 0, y: -20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: 'easeOut' } },
};

// Usage on container:
<motion.div
  initial="hidden"
  whileInView="visible"
  viewport={{ once: true, amount: 0.1 }}
  variants={sectionVariants}
>
  <motion.div variants={itemVariants}>...</motion.div>
</motion.div>
```

<!-- Theme tokens (from theme.css): -->
```css
--primary: #7c3aed;          /* violet */
--neon-purple: #a78bfa;      /* light violet */
--neon-violet: #7c3aed;
--card: #0d0d1a;
```

<!-- DifferentialsSection background pattern: -->
```tsx
<div className="absolute inset-0">
  <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-violet-600/10 rounded-full blur-3xl"></div>
  <div className="absolute top-0 left-1/4 w-px h-full bg-gradient-to-b from-transparent via-violet-500/20 to-transparent"></div>
  <div className="absolute top-0 right-1/4 w-px h-full bg-gradient-to-b from-transparent via-purple-500/20 to-transparent"></div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NewsSection with violet palette and scroll animations</name>
  <files>src/app/components/NewsSection.tsx</files>
  <action>
Fully rewrite NewsSection.tsx. Preserve existing newsArticles data array and component logic. Change every visual element from blue/cyan to the violet/purple brand palette. Add motion/react scroll entrance animations matching DifferentialsSection's pattern.

Specific changes required:

1. Add `import { motion } from 'motion/react';` (keep existing ArrowRight, Calendar imports).

2. Define three variant objects above the component (same pattern as DifferentialsSection):
   - `headerVariants`: hidden `{ opacity: 0, y: -20 }`, visible `{ opacity: 1, y: 0, duration: 0.5 }`
   - `containerVariants`: hidden `{}`, visible `{ staggerChildren: 0.15 }`
   - `cardVariants`: hidden `{ opacity: 0, y: 30 }`, visible `{ opacity: 1, y: 0, duration: 0.6, ease: 'easeOut' }` — use y:30 (cards slide up, distinct from left-slide used in Differentials)

3. Background decorative elements: replace `via-blue-500` and `via-cyan-500` with `via-violet-500/20` lines (match Differentials pattern: top-0 left-1/4 and right-1/4 vertical lines). Add a blurred radial glow: `bg-violet-600/10 rounded-full blur-3xl w-[600px] h-[600px]` centered.

4. Section badge pill: `bg-gradient-to-r from-violet-500/10 to-purple-500/10 border border-violet-500/30` with `text-violet-300` label and `bg-violet-400 animate-pulse` dot. Wrap badge + h2 + divider in `motion.div` with `headerVariants`, `whileInView`, `viewport={{ once: true, amount: 0.3 }}`.

5. Section h2: `font-extrabold` (match Phase 3 convention). Gradient: `from-violet-400 to-fuchsia-500`. Divider line: `from-violet-500 to-purple-600`.

6. News grid: wrap the `div.grid` in `motion.div` with `containerVariants`, `whileInView`, `viewport={{ once: true, amount: 0.1 }}`.

7. Each news card: change from plain `div` to `motion.div` with `variants={cardVariants}`. Card background: `from-[#0d0d1a] to-[#110d22]`. Border: `border-violet-500/20 hover:border-violet-400/50`. Hover shadow: `hover:shadow-[0_0_25px_rgba(124,58,237,0.25)]`. Remove `hover:-translate-y-2` (motion handles animation).

8. Card title hover: `group-hover:text-violet-400` (was blue-400).

9. "Leia mais" button: `text-violet-400 hover:text-violet-300` (was blue).

10. Bottom gradient bar on card: `from-violet-500 to-purple-600` (was blue/cyan).

Keep the existing card structure (date, title, summary, read-more button) intact. Do not change the newsArticles data — copy review happens in plan 03.
  </action>
  <verify>
    <automated>cd "C:\Users\jorda\Downloads\luknet\Luknet-main\Luknet-main" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>NewsSection.tsx compiles without TypeScript errors. File contains "from 'motion/react'", "whileInView", "violet" (no remaining "blue-400", "blue-500", "cyan-400", "cyan-500", "cyan-600" color tokens), and "cardVariants".</done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` — zero TypeScript errors in NewsSection.tsx
2. Grep check: `grep -n "blue-\|cyan-" src/app/components/NewsSection.tsx` should return zero matches on color token lines
3. Grep check: `grep "whileInView" src/app/components/NewsSection.tsx` should return at least 2 matches (header + grid)
4. Visual: Start dev server, scroll to #noticias section — cards should animate in from below with stagger, header should slide down first
</verification>

<success_criteria>
- NewsSection has no blue or cyan color tokens
- Section header and card grid each have their own whileInView motion containers
- Cards use cardVariants (y: 30 slide-up) distinct from Differentials (x: -30 slide-left)
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-completion/04-01-SUMMARY.md`
</output>
