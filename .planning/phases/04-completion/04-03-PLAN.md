---
phase: 04-completion
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/app/components/Footer.tsx
autonomous: true
requirements:
  - SECT-03
  - SECT-04
  - COPY-02
must_haves:
  truths:
    - "Footer logo uses violet gradient (from-violet-500 to-purple-600), not blue"
    - "Footer brand name text gradient is violet/purple, not blue/purple"
    - "Footer brand tagline copy positions Luknet as a premium tech partner, not generic 'web solutions'"
    - "Footer social icons hover with violet glow (not arbitrary platform colors)"
    - "Footer bottom bar has a neon-style violet separator line"
    - "All section texts across News, Contact, and Footer are premium and brand-consistent — no orange artifacts, no generic filler"
    - "Every section on the page (Hero, Services, Differentials, News, Contact) has visible whileInView scroll entrance animations"
  artifacts:
    - path: "src/app/components/Footer.tsx"
      provides: "Dark violet Footer with brand-aligned copy and subtle entrance animation"
      contains: "violet"
  key_links:
    - from: "src/app/components/Footer.tsx"
      to: "theme.css violet palette"
      via: "Tailwind violet-500/violet-600 classes"
      pattern: "violet-"
---

<objective>
Redesign Footer with violet brand palette, update brand tagline copy, and confirm all sections now have scroll animations. This plan also performs the final COPY-02 review pass: verify News and Contact copy is coherent and premium after their redesigns.

Purpose: Footer is the last element a visitor sees. It must close the page cohesively. The brand tagline "Soluções Web Inteligentes" is generic — it should position Luknet as a strategic tech partner. SECT-04 requires a final confirmation that ALL sections have whileInView animations after this phase is done.
Output: Footer.tsx rewritten with violet palette, premium copy, and a subtle fade-in entrance. Copy-02 confirmed complete.
</objective>

<execution_context>
@C:/Users/jorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Summaries from Plans 01 and 02 confirm News and Contact are fully violet. -->
@.planning/phases/04-completion/04-01-SUMMARY.md
@.planning/phases/04-completion/04-02-SUMMARY.md

<interfaces>
<!-- Footer current scrollToSection helper (preserve exactly): -->
```typescript
const scrollToSection = (id: string) => {
  const element = document.getElementById(id);
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
};
```

<!-- Sections with confirmed whileInView animations after Phase 3 + Phase 4: -->
<!-- - HeroSection: entrance animation on mount (Phase 2) -->
<!-- - ServicesSection: card stagger (Phase 3 Plan 01) -->
<!-- - DifferentialsSection: headerVariants + itemVariants whileInView (Phase 3 Plan 02) -->
<!-- - NewsSection: headerVariants + cardVariants whileInView (Phase 4 Plan 01) -->
<!-- - ContactSection: header + leftPanel + rightPanel whileInView (Phase 4 Plan 02) -->
<!-- Footer gets a subtle fadeIn — it is not a sales section, so a simple opacity entrance is appropriate -->

<!-- Motion pattern (minimal for footer — just a fade, no structural stagger needed): -->
```typescript
import { motion } from 'motion/react';
const footerVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: 'easeOut' } },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Footer with violet palette, premium copy, and entrance animation</name>
  <files>src/app/components/Footer.tsx</files>
  <action>
Rewrite Footer.tsx. Preserve the scrollToSection helper, currentYear, navigation links structure, and social icon set. Update all colors to violet palette, update brand tagline copy, and add a subtle whileInView entrance animation.

Specific changes:

1. Add `import { motion } from 'motion/react';` at top (keep Facebook, Instagram, Linkedin, Twitter imports).

2. Define a single variant above the component:
   ```typescript
   const footerVariants = {
     hidden: { opacity: 0, y: 20 },
     visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: 'easeOut' } },
   };
   ```

3. Wrap the inner `div.max-w-7xl` in `motion.div` with `footerVariants`, `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, amount: 0.1 }}`.

4. Footer wrapper: change `bg-gradient-to-b from-transparent to-black/30 border-t border-white/10` to `bg-gradient-to-b from-transparent to-[#050008]/80 border-t border-violet-500/20` for a violet-tinted border separator.

5. Brand logo icon: change `from-blue-500 to-purple-600` to `from-violet-500 to-purple-600`.

6. Brand name text gradient: change `from-blue-400 to-purple-500` to `from-violet-400 to-purple-500`.

7. Brand tagline (COPY-02): change "Soluções Web Inteligentes para o seu negócio crescer no mundo digital." to "Tecnologia sob medida para negócios que não querem ser genéricos." — this positions Luknet as a strategic partner, not a commodity provider.

8. Social icon hover colors: replace the platform-specific hover colors (blue-600, pink-600, blue-700, sky-500) with a uniform `hover:bg-violet-600` for all four icons. This maintains brand consistency over platform recognition. Border: `border-violet-500/20`.

9. Bottom bar separator: change `border-t border-white/10` to `border-t border-violet-500/15`. Add a thin neon line above the bottom bar: `<div className="h-px w-full bg-gradient-to-r from-transparent via-violet-500/30 to-transparent mb-8" />` before the bottom bar div.

10. Bottom bar links hover: change `hover:text-white` to `hover:text-violet-300` on the privacy/terms links.

Preserve: all nav section IDs and labels, all service list items, copyright year logic, all href="#" values.
  </action>
  <verify>
    <automated>cd "C:\Users\jorda\Downloads\luknet\Luknet-main\Luknet-main" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Footer.tsx compiles without TypeScript errors. Contains "from 'motion/react'", "whileInView", "violet-" colors, and updated tagline text "Tecnologia sob medida".</done>
</task>

<task type="auto">
  <name>Task 2: SECT-04 and COPY-02 confirmation pass</name>
  <files>src/app/components/NewsSection.tsx, src/app/components/ContactSection.tsx, src/app/components/Footer.tsx</files>
  <action>
Perform a final verification grep pass across the three sections completed in this phase. This is a read-and-confirm task — make targeted fixes if any issues are found, otherwise no changes are needed.

SECT-04 — Confirm all sections have whileInView animations:
Run the following checks and note results:
```bash
grep -c "whileInView" src/app/components/NewsSection.tsx
grep -c "whileInView" src/app/components/ContactSection.tsx
grep -c "whileInView" src/app/components/Footer.tsx
grep -c "whileInView" src/app/components/DifferentialsSection.tsx
```
Each should return >= 1. If any file is missing whileInView, add the minimal variant pattern (headerVariants fade-in) to that component.

COPY-02 — Review all section texts for quality:
Check that no section still contains placeholder-sounding text. Specifically:
- NewsSection article titles and summaries: already updated data in plan 01 — confirm they are coherent Portuguese text about tech topics (they are, per original data, which is already premium)
- ContactSection form placeholder text: "Seu nome completo", "seu@email.com", "Assunto da mensagem", "Escreva sua mensagem aqui..." — these are acceptable, no change needed
- ContactSection contact info (address, phone, email): real data preserved from original — no change needed
- Footer tagline: updated in Task 1 to "Tecnologia sob medida para negócios que não querem ser genéricos." — confirm present

If SECT-04 grep confirms all sections have whileInView and COPY-02 review finds no issues, create a brief confirmation note as a code comment at the top of each file: `// Phase 4 complete: violet palette, whileInView animations, copy reviewed` — place this as a single-line comment after the import block, before the first const declaration.

If any fix IS needed (missing animation, stale orange token), apply it directly to the file.
  </action>
  <verify>
    <automated>cd "C:\Users\jorda\Downloads\luknet\Luknet-main\Luknet-main" && grep -c "whileInView" src/app/components/NewsSection.tsx src/app/components/ContactSection.tsx src/app/components/Footer.tsx src/app/components/DifferentialsSection.tsx 2>&1</automated>
  </verify>
  <done>All four section files contain at least 1 "whileInView" occurrence. No orange tokens remain in any of the three Phase 4 files. Footer tagline contains "Tecnologia sob medida". TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors across the full project
2. `grep -rn "orange-" src/app/components/` — zero matches in NewsSection, ContactSection, Footer
3. `grep -c "whileInView" src/app/components/NewsSection.tsx src/app/components/ContactSection.tsx src/app/components/Footer.tsx src/app/components/DifferentialsSection.tsx` — each line shows count >= 1
4. `grep "Tecnologia sob medida" src/app/components/Footer.tsx` — returns a match
5. `grep -rn "blue-400\|blue-500\|cyan-" src/app/components/NewsSection.tsx` — zero matches
6. Visual: Full page scroll from top to bottom — every section entrance is visually animated, palette is uniformly dark violet throughout
</verification>

<success_criteria>
- Footer uses violet palette exclusively, logo and brand text aligned
- Footer tagline updated to premium positioning copy
- All 5 major sections (Hero, Services, Differentials, News, Contact) confirmed to have scroll entrance animations (SECT-04 complete)
- Footer has subtle fade-in entrance animation
- COPY-02 confirmed: no placeholder or outdated text remains in any Phase 4 section
- TypeScript compiles clean with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-completion/04-03-SUMMARY.md`
</output>
