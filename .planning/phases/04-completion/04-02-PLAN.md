---
phase: 04-completion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/ContactSection.tsx
autonomous: true
requirements:
  - SECT-02
must_haves:
  truths:
    - "ContactSection has zero orange color tokens — accent is fully violet/purple"
    - "ContactSection form fields have violet focus rings, not generic purple"
    - "ContactSection send button uses violet gradient, not orange-to-purple"
    - "Contact info icon badges use violet gradient instead of blue/orange/purple mix"
    - "ContactSection header, form panel, and info panel each animate in when scrolled into view"
  artifacts:
    - path: "src/app/components/ContactSection.tsx"
      provides: "Fully redesigned dark violet contact section with scroll animations and consistent futuristic form styling"
      contains: "motion/react"
  key_links:
    - from: "src/app/components/ContactSection.tsx"
      to: "motion/react"
      via: "import { motion } from 'motion/react'"
      pattern: "whileInView"
---

<objective>
Redesign ContactSection to eliminate orange color artifacts and align fully with the violet/purple brand palette. Add whileInView scroll entrance animations to the section header and both layout columns.

Purpose: ContactSection currently uses an orange gradient on the submit button and an orange/purple headline that clashes with the violet-only identity established across all other sections.
Output: src/app/components/ContactSection.tsx rewritten with consistent violet palette, futuristic form field styling, and scroll-triggered animations.
</objective>

<execution_context>
@C:/Users/jorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current ContactSection form state (preserve exactly): -->
```typescript
const [formData, setFormData] = useState({
  name: '',
  email: '',
  subject: '',
  message: '',
});

const handleSubmit = (e: React.FormEvent) => { ... }
const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => { ... }
```

<!-- Animation pattern (from DifferentialsSection.tsx, Phase 3): -->
```typescript
import { motion } from 'motion/react';

const headerVariants = {
  hidden: { opacity: 0, y: -20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: 'easeOut' } },
};

// For form and info columns — slide in from sides:
const leftPanelVariants = {
  hidden: { opacity: 0, x: -40 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.7, ease: 'easeOut' } },
};
const rightPanelVariants = {
  hidden: { opacity: 0, x: 40 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.7, ease: 'easeOut', delay: 0.15 } },
};
```

<!-- Theme tokens: -->
```css
--primary: #7c3aed;
--card: #0d0d1a;        /* bg for panels */
--border: rgba(124, 58, 237, 0.2);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ContactSection with violet palette and scroll animations</name>
  <files>src/app/components/ContactSection.tsx</files>
  <action>
Rewrite ContactSection.tsx preserving all form logic (useState, handleSubmit, handleChange, formData shape) and contact info data (address, phone, email, hours). Change visual styling to violet brand palette and add motion/react scroll animations.

Specific changes:

1. Add `import { motion } from 'motion/react';` at top (keep useState, MapPin, Phone, Mail, Send imports).

2. Define variant objects above the component:
   - `headerVariants`: `{ hidden: { opacity: 0, y: -20 }, visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: 'easeOut' } } }`
   - `leftPanelVariants`: `{ hidden: { opacity: 0, x: -40 }, visible: { opacity: 1, x: 0, transition: { duration: 0.7, ease: 'easeOut' } } }` — form slides in from left
   - `rightPanelVariants`: `{ hidden: { opacity: 0, x: 40 }, visible: { opacity: 1, x: 0, transition: { duration: 0.7, ease: 'easeOut', delay: 0.15 } } }` — info slides in from right

3. Background blobs: remove `bg-orange-600/20`. Use two violet blobs: `bg-violet-600/15 rounded-full blur-3xl` (top-right) and `bg-purple-800/15 rounded-full blur-3xl` (bottom-left).

4. Section header: Add a badge pill above h2 (matching other sections): `bg-gradient-to-r from-violet-500/10 to-purple-500/10 border border-violet-500/30 rounded-full` with `text-violet-300` label "Fale Conosco" and pulsing `bg-violet-400` dot. Wrap header block in `motion.div` with `headerVariants`, `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, amount: 0.3 }}`. h2 gradient: `from-violet-400 to-fuchsia-500`. Divider: `from-violet-500 to-purple-600`.

5. Form panel (left column): wrap the entire form container div in `motion.div` with `leftPanelVariants`, `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, amount: 0.2 }}`. Panel background: `bg-gradient-to-br from-[#0d0d1a] to-[#110d22] border border-violet-500/20`. Input fields: change `focus:border-purple-500` to `focus:border-violet-500` and add `focus:shadow-[0_0_0_2px_rgba(124,58,237,0.2)]` for glow effect. Keep `bg-white/5` for input backgrounds.

6. Submit button: replace the orange gradient (`from-orange-500 to-purple-600`) with `bg-gradient-to-r from-violet-600 to-purple-700 hover:from-violet-500 hover:to-purple-600`. Shadow: `shadow-violet-500/30 hover:shadow-violet-500/50`.

7. Info panels (right column): wrap the right column div in `motion.div` with `rightPanelVariants`, same `initial/whileInView/viewport`. Panel backgrounds: `bg-gradient-to-br from-[#0d0d1a] to-[#110d22] border border-violet-500/20`. All three contact icon badges: use `bg-gradient-to-br from-violet-500 to-purple-600` (remove orange/pink badge variants — every icon gets the same violet treatment).

8. h4 font size for "Horário de Atendimento": add `text-lg` for visual hierarchy.

Preserve: form field ids, names, values, onChange, onSubmit, required attrs, all placeholder text, all contact info data (address, phone, email, hours).
  </action>
  <verify>
    <automated>cd "C:\Users\jorda\Downloads\luknet\Luknet-main\Luknet-main" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ContactSection.tsx compiles without TypeScript errors. Contains "from 'motion/react'", "whileInView", "violet-500" or "violet-600" on submit button. Zero occurrences of "orange-" color tokens.</done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` — zero TypeScript errors
2. Grep check: `grep -n "orange-" src/app/components/ContactSection.tsx` returns zero matches
3. Grep check: `grep "whileInView" src/app/components/ContactSection.tsx` returns at least 3 matches (header + left panel + right panel)
4. Visual: Start dev server, scroll to #contato — form panel slides in from left, info panel from right, staggered by 150ms. Submit button is violet, not orange.
</verification>

<success_criteria>
- Zero orange color tokens remaining in ContactSection.tsx
- Three separate whileInView animation triggers: header, form column, info column
- Form functionality preserved: typing updates state, submit fires handleSubmit
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-completion/04-02-SUMMARY.md`
</output>
